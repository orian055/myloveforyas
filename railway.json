{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile",
    "buildCommand": "dotnet publish -c Release -o /app"
  },
  "deploy": {
    "startCommand": "dotnet MinimalApp.dll",
    "restartPolicyMaxRetries": 5,
    "restartPolicyWindowSeconds": 600
  }
}
